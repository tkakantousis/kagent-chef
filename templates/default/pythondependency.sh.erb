#!/bin/bash

if [ $# -ne 2  ]; then
 echo "incorrect usage"
 exit 1
fi

set -e

# Name of hopsworks project
PROJECT="$1"
# 'add' or 'rm'
OP="$2"
# Name of virtualenv python dependency
PACKAGE="$3"

# 0. Load environment $PROJECT


# 1. Check if package is already installed for this environment, if adding it


# 2. Install or remove package

#virtualenv install $PACKAGE
#virtualenv rm $PACKAGE
